name: Student 3 - CI

on:
  push:
    branches: [main]
    paths:
      - "student-3/backend/**"
      - "student-3/contracts/**"
      - "student-3/database/**"
      - "student-3/frontend/**"
      - "shared/ui-kit/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/student-3-ci.yml"
  pull_request:
    paths:
      - "student-3/backend/**"
      - "student-3/contracts/**"
      - "student-3/database/**"
      - "student-3/frontend/**"
      - "shared/ui-kit/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/student-3-ci.yml"

# Cancel superseded runs on the same branch/PR to save CI minutes.
concurrency:
  group: student-3-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  DOTNET_VERSION: "10.0.x"
  NODE_VERSION: "22"
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  DOTNET_SOLUTION: student-3/backend/DeadlineTaskTracker.sln
  API_PROJECT: student-3/backend/Api/Api.csproj
  DATABASE_PROJECT: student-3/database/Database/Database.csproj

jobs:
  frontend-build:
    name: Frontend type check & production build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install workspace dependencies
        run: npm ci

      - name: Type check and build
        run: npm run build --workspace=student-3-frontend

  build-and-analyze:
    name: Build, analyze & format check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('student-3/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore "${{ env.DOTNET_SOLUTION }}"

      - name: Build (Release, warnings as errors)
        run: dotnet build "${{ env.DOTNET_SOLUTION }}" --configuration Release --no-restore -warnaserror

      - name: Verify code formatting
        run: dotnet format "${{ env.DOTNET_SOLUTION }}" --verify-no-changes --severity warn

  frontend-backend-contract:
    name: API contract & browser CORS smoke test
    needs: [frontend-build, build-and-analyze]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore and build services
        run: |
          dotnet restore "${{ env.DOTNET_SOLUTION }}"
          dotnet build "${{ env.DOTNET_SOLUTION }}" --configuration Release --no-restore -warnaserror

      - name: Verify frontend task contract and CORS
        shell: bash
        run: |
          set -euo pipefail

          database_file="$RUNNER_TEMP/student-3-ci.db"
          ASPNETCORE_URLS=http://127.0.0.1:5203 \
          ASPNETCORE_ENVIRONMENT=Production \
          ConnectionStrings__DefaultConnection="Data Source=$database_file" \
          dotnet run \
            --project "${{ env.DATABASE_PROJECT }}" \
            --configuration Release \
            --no-build \
            --no-launch-profile > database.log 2>&1 &
          database_pid=$!

          for _ in {1..30}; do
            if curl --fail --silent http://127.0.0.1:5203/health/ready > /dev/null; then
              break
            fi
            if ! kill -0 "$database_pid" 2>/dev/null; then
              cat database.log
              exit 1
            fi
            sleep 1
          done
          curl --fail --silent http://127.0.0.1:5203/health/ready > /dev/null

          ASPNETCORE_URLS=http://127.0.0.1:5103 \
          ASPNETCORE_ENVIRONMENT=Production \
          DatabaseService__BaseUrl=http://127.0.0.1:5203 \
          SharedService__BaseUrl=http://127.0.0.1:5110 \
          AiGateway__BaseUrl=http://127.0.0.1:5200 \
          NotificationService__BaseUrl=http://127.0.0.1:5101 \
          DueSoonReminder__IntervalMinutes=60 \
          dotnet run \
            --project "${{ env.API_PROJECT }}" \
            --configuration Release \
            --no-build \
            --no-launch-profile \
            > api.log 2>&1 &
          api_pid=$!
          task_id=""

          cleanup() {
            if [[ -n "$task_id" ]]; then
              curl --silent --request DELETE \
                "http://127.0.0.1:5103/api/tasks/$task_id" > /dev/null || true
            fi
            kill "$api_pid" 2>/dev/null || true
            kill "$database_pid" 2>/dev/null || true
          }
          trap cleanup EXIT

          for _ in {1..30}; do
            if curl --fail --silent http://127.0.0.1:5103/health/live > /dev/null &&
              curl --fail --silent \
                http://127.0.0.1:5103/api/courses/ > courses.json; then
              break
            fi
            if ! kill -0 "$api_pid" 2>/dev/null; then
              cat api.log
              exit 1
            fi
            sleep 1
          done
          test -s courses.json

          # The process is live, but required microservices are unavailable in this job.
          readiness_status=$(
            curl --silent --output /dev/null --write-out "%{http_code}" \
              http://127.0.0.1:5103/health/ready
          )
          test "$readiness_status" = "503"

          created=$(
            curl --fail --silent \
              --request POST \
              --header "Content-Type: application/json" \
              --data '{
                "title": "CI frontend contract",
                "description": "Created with the frontend request shape",
                "dueDate": "2030-01-02T03:04:05Z",
                "priority": "High",
                "courseId": null,
                "parentTaskId": null
              }' \
              http://127.0.0.1:5103/api/tasks/
          )
          task_id=$(jq --exit-status --raw-output '.id' <<< "$created")
          jq --exit-status '
            .title == "CI frontend contract" and
            .priority == "High" and
            .status == "Todo" and
            .dueDate != null
          ' <<< "$created" > /dev/null

          updated=$(
            curl --fail --silent \
              --request PUT \
              --header "Content-Type: application/json" \
              --data '{
                "newTitle": null,
                "updateDescription": false,
                "newDescription": null,
                "updateDueDate": true,
                "newDueDate": null,
                "newPriority": "Medium",
                "newStatus": "InProgress"
              }' \
              "http://127.0.0.1:5103/api/tasks/$task_id"
          )
          jq --exit-status '
            .dueDate == null and
            .priority == "Medium" and
            .status == "InProgress"
          ' <<< "$updated" > /dev/null

          curl --fail --silent \
            --request OPTIONS \
            --header "Origin: http://localhost:3003" \
            --header "Access-Control-Request-Method: PUT" \
            --dump-header cors-headers.txt \
            --output /dev/null \
            "http://127.0.0.1:5103/api/tasks/$task_id"
          grep --ignore-case \
            "^Access-Control-Allow-Origin: http://localhost:3003" \
            cors-headers.txt
          grep --ignore-case \
            "^Access-Control-Allow-Methods:.*PUT" \
            cors-headers.txt

  ef-core-migrations-check:
    name: EF Core migrations drift check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('student-3/**/*.csproj') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore "${{ env.DATABASE_PROJECT }}"

      - name: Install dotnet-ef (matching project's EF Core version)
        run: dotnet tool install --global dotnet-ef --version 10.0.11

      # Fails the build if the current model has diverged from the latest
      # migration snapshot (e.g. an entity property changed without a
      # corresponding `dotnet ef migrations add`).
      - name: Check for pending model changes
        run: dotnet ef migrations has-pending-model-changes --project "${{ env.DATABASE_PROJECT }}"

  vulnerability-scan:
    name: NuGet vulnerability scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore "${{ env.DOTNET_SOLUTION }}"

      # `dotnet list package --vulnerable` exits 0 even when vulnerabilities are
      # found, so grep the output to fail the job when any are reported.
      - name: Scan for known-vulnerable packages
        run: |
          set -o pipefail
          dotnet list "${{ env.DOTNET_SOLUTION }}" package --vulnerable --include-transitive | tee vuln-report.txt
          if grep -qi "has the following vulnerable packages" vuln-report.txt; then
            echo "::error::Vulnerable NuGet packages detected. See log above."
            exit 1
          fi
