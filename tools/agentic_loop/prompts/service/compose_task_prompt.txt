Review Target: {{REVIEW_TARGET}}

The evidence below is parsed directly from the repo-root docker-compose.yml
- the actual services, ports, volumes, dependencies, and networks defined
today, not an assumed topology.

Compose Evidence:
{{VALIDATION_EVIDENCE}}

Task:
Analyze the evidence above. Identify ONE precise, evidence-backed
observation about the compose architecture, covering at most one of:
- A missing `depends_on` for a service that clearly needs another
- A port conflict or an unpublished port for a service that likely needs one
- A service with no volume where persistence looks required
- A service not attached to a shared network when others are

If nothing is supported by the evidence, that is a valid and good outcome
- say exactly: "No evidence-backed improvement identified."

Output Format:
- Return exactly two bullet points (Issue + Recommendation), or the
  no-evidence sentence.
- Maximum 100 words total.
- No explanations, no reasoning.
