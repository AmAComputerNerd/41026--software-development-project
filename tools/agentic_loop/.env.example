# Use to override the default API base (when not running in Docker).
API_BASE_URL_STUDENT_3=http://localhost:5014

# Use to override the default frontend dev/preview server base (when not
# running in Docker). Only needed for owners with a Vue + Vuetify frontend.
# Example: FRONTEND_BASE_URL_STUDENT_3=http://localhost:5173

# Model config
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.1:8b
OLLAMA_REVIEW_MODEL=deepseek-r1:8b
